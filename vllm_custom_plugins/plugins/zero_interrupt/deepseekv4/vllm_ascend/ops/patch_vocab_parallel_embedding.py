## Copyright (c) 2025 Huawei Technologies Co., Ltd. All Rights Reserved.# This file is a part of the vllm-ascend project.## Licensed under the Apache License, Version 2.0 (the "License");# you may not use this file except in compliance with the License.# You may obtain a copy of the License at##     http://www.apache.org/licenses/LICENSE-2.0## Unless required by applicable law or agreed to in writing, software# distributed under the License is distributed on an "AS IS" BASIS,# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.# See the License for the specific language governing permissions and# limitations under the License.#import torchfrom torch import nnfrom torch.nn.parameter import Parameterfrom vllm.distributed import dividefrom vllm.distributed.parallel_state import get_tp_groupfrom vllm.model_executor.layers.logits_processor import LogitsProcessorfrom vllm.model_executor.layers.quantization.base_config import (    QuantizationConfig,    QuantizeMethodBase,    method_has_implemented_embedding,)from vllm.model_executor.layers.vocab_parallel_embedding import (    DEFAULT_VOCAB_PADDING_SIZE,    ParallelLMHead,    UnquantizedEmbeddingMethod,    VocabParallelEmbedding,    VocabParallelEmbeddingShardIndices,    pad_vocab_size,    vocab_range_from_global_vocab_size,    vocab_range_from_global_vocab_size_uneven,)from vllm.model_executor.utils import set_weight_attrsfrom vllm_ascend.distributed.parallel_state import get_embed_tp_group, get_lmhead_tp_groupfrom vllm_ascend.utils import embedding_tp_enable, lmhead_tp_enablefrom vllm_ascend.ops.vocab_parallel_embedding import (    AscendVocabParallelEmbedding,    AscendParallelLMHead,    AscendLogitsProcessor)from vllm_custom_plugins.plugins.zero_interrupt.deepseekv4.vllm.model_executor.layers.patch_logits_processor import PatchLogitsProcessorimport loggingerrorlogger = logging.getLogger(__name__)class PatchAscendVocabParallelEmbedding(AscendVocabParallelEmbedding):    """    Register VocabParallelEmbedding as a custom op for Ascend.    AscendVocabParallelEmbedding support different communication parallel groups    Added the feature of lmheadTP in pure dp scenario    """    def __init__(            self,            num_embeddings: int,            embedding_dim: int,            params_dtype: torch.dtype | None = None,            org_num_embeddings: int | None = None,            padding_size: int = DEFAULT_VOCAB_PADDING_SIZE,            quant_config: QuantizationConfig | None = None,            prefix: str = "",            uneven_split: bool = False,  # [h30014172] 新增：非对称TP切分支持    ):        nn.Module.__init__(self)        self.uneven_split = uneven_split  # [h30014172]        #errorlogger.debug(f"AscendVocabParallelEmbedding--uneven_split:{self.uneven_split} [h30014172]")        self.forward_type = None        if lmhead_tp_enable() and "head" in prefix:            self.comm_group = get_lmhead_tp_group()        elif embedding_tp_enable() and "embed_tokens" in prefix:            self.comm_group = get_embed_tp_group()            self.forward_type = "embed_tp"        else:            self.comm_group = get_tp_group()        self.tp_size = self.comm_group.world_size        self.tp_rank = self.comm_group.rank_in_group        self.num_embeddings = num_embeddings        self.padding_size = padding_size        self.org_vocab_size = org_num_embeddings or num_embeddings        num_added_embeddings = num_embeddings - self.org_vocab_size        self.org_vocab_size_padded = pad_vocab_size(self.org_vocab_size, self.padding_size)        self.num_embeddings_padded = pad_vocab_size(            self.org_vocab_size_padded + num_added_embeddings, self.padding_size        )        assert self.org_vocab_size_padded <= self.num_embeddings_padded        # [h30014172] 透传 uneven_split 给上游 _get_indices        self.shard_indices = PatchAscendVocabParallelEmbedding._get_indices(            self.num_embeddings_padded,            self.org_vocab_size_padded,            self.num_embeddings,            self.org_vocab_size,            self.tp_rank,            self.tp_size,            uneven_split        )        self.embedding_dim = embedding_dim        quant_method = None        if quant_config is not None:            quant_method = quant_config.get_quant_method(self, prefix=prefix)        if quant_method is None:            quant_method = UnquantizedEmbeddingMethod()        is_embedding_layer = type(self) is VocabParallelEmbedding        quant_method_implements_embedding = method_has_implemented_embedding(type(quant_method))        if is_embedding_layer and not quant_method_implements_embedding:            raise NotImplementedError(                f"The class {type(quant_method).__name__} must implement "                "the 'embedding' method, see UnquantizedEmbeddingMethod."            )        self.quant_method: QuantizeMethodBase = quant_method        if params_dtype is None:            params_dtype = torch.get_default_dtype()        self.params_dtype = params_dtype        self.num_added_embeddings = self.num_embeddings - self.org_vocab_size        self.num_embeddings_per_partition = self.shard_indices.num_elements_padded        self.num_org_embeddings_per_partition = (                self.shard_indices.org_vocab_end_index - self.shard_indices.org_vocab_start_index        )        self.num_added_embeddings_per_partition = (                self.shard_indices.added_vocab_end_index - self.shard_indices.added_vocab_start_index        )        self.quant_method.create_weights(            self,            self.embedding_dim,            [self.num_embeddings_per_partition],            self.embedding_dim,            self.num_embeddings_padded,            params_dtype=params_dtype,            weight_loader=self.weight_loader,        )    @classmethod    def _get_indices(        cls,        vocab_size_padded: int,        org_vocab_size_padded: int,        vocab_size: int,        org_vocab_size: int,        tp_rank: int,        tp_size: int,        uneven_split: bool = False    ) -> VocabParallelEmbeddingShardIndices:        """Get start and end indices for vocab parallel embedding."""        if uneven_split:            padded_org_vocab_start_index, padded_org_vocab_end_index = (                vocab_range_from_global_vocab_size_uneven(org_vocab_size_padded, tp_rank, tp_size)            )            num_added_embeddings_padded = vocab_size_padded - org_vocab_size_padded            padded_added_vocab_start_index, padded_added_vocab_end_index = (                vocab_range_from_global_vocab_size_uneven(                    num_added_embeddings_padded, tp_rank, tp_size, offset=org_vocab_size                )            )        else:            padded_org_vocab_start_index, padded_org_vocab_end_index = (                vocab_range_from_global_vocab_size(org_vocab_size_padded, tp_rank, tp_size)            )            num_added_embeddings_padded = vocab_size_padded - org_vocab_size_padded            padded_added_vocab_start_index, padded_added_vocab_end_index = (                vocab_range_from_global_vocab_size(                    num_added_embeddings_padded, tp_rank, tp_size, offset=org_vocab_size                )            )        org_vocab_start_index = min(padded_org_vocab_start_index, org_vocab_size)        org_vocab_end_index = min(padded_org_vocab_end_index, org_vocab_size)        added_vocab_start_index = min(padded_added_vocab_start_index, vocab_size)        added_vocab_end_index = min(padded_added_vocab_end_index, vocab_size)        return VocabParallelEmbeddingShardIndices(            padded_org_vocab_start_index,            padded_org_vocab_end_index,            padded_added_vocab_start_index,            padded_added_vocab_end_index,            org_vocab_start_index,            org_vocab_end_index,            added_vocab_start_index,            added_vocab_end_index,        )    def _get_masked_input_and_mask(        self,        input_: torch.Tensor,        org_vocab_start_index: int,        org_vocab_end_index: int,        num_org_vocab_padding: int,        added_vocab_start_index: int,        added_vocab_end_index: int,    ) -> tuple[torch.Tensor, torch.Tensor]:        # torch.compile will fuse all of the pointwise ops below        # into a single kernel, making it very fast        org_vocab_mask = (input_ >= org_vocab_start_index) & (input_ < org_vocab_end_index)        # Adapt: avoid create added_vocab_mask when added_vocab_start_index == added_vocab_end_index.        if added_vocab_start_index == added_vocab_end_index:            valid_offset = org_vocab_start_index * org_vocab_mask            vocab_mask = org_vocab_mask        else:            added_vocab_mask = (input_ >= added_vocab_start_index) & (input_ < added_vocab_end_index)            added_offset = (                added_vocab_start_index - (org_vocab_end_index - org_vocab_start_index) - num_org_vocab_padding            )            valid_offset = (org_vocab_start_index * org_vocab_mask) + (added_offset * added_vocab_mask)            vocab_mask = org_vocab_mask | added_vocab_mask        # Adapt end.        input_ = vocab_mask * (input_ - valid_offset)        return input_, ~vocab_mask    def forward(self, input_):        if self.forward_type == "embed_tp":            return self._forward_embed_tp(input_)        else:            return self._forward_origin(input_)    def _forward_embed_tp(self, input_):        complete_input = self.comm_group.all_gather(input_, dim=0)        masked_input, input_mask = self._get_masked_input_and_mask(            complete_input,            self.shard_indices.org_vocab_start_index,            self.shard_indices.org_vocab_end_index,            self.shard_indices.num_org_vocab_padding,            self.shard_indices.added_vocab_start_index,            self.shard_indices.added_vocab_end_index,        )        # Get the embeddings.        output_parallel = self.quant_method.embedding(self, masked_input.long())        output_parallel.masked_fill_(input_mask.unsqueeze(-1), 0)        output = self.comm_group.reduce_scatter(output_parallel, dim=0)        output = output.view(input_.shape[0], -1)        return output    def _forward_origin(self, input_):        if self.tp_size > 1:            # Build the mask.            masked_input, input_mask = self._get_masked_input_and_mask(                input_,                self.shard_indices.org_vocab_start_index,                self.shard_indices.org_vocab_end_index,                self.shard_indices.num_org_vocab_padding,                self.shard_indices.added_vocab_start_index,                self.shard_indices.added_vocab_end_index,            )            #errorlogger.debug(f"[TP {self.tp_rank}] vocab_parallel_embedding input shape={input_.shape}, masked_input shape={masked_input.shape}, masked_input min={masked_input.min().item()}, max={masked_input.max().item()}")            #errorlogger.debug(f"[TP {self.tp_rank}] PREFILL input_ unique values (first 20): {input_.unique()[:20].tolist()}")            #errorlogger.debug(f"[TP {self.tp_rank}] PREFILL input_ min={input_.min().item()}, max={input_.max().item()}")        else:            masked_input = input_        # Get the embeddings.        output_parallel = self.quant_method.embedding(self, masked_input.long())        #errorlogger.debug(f"[TP {self.tp_rank}] embedding output_parallel has inf={torch.isinf(output_parallel).any().item()}, has nan={torch.isnan(output_parallel).any().item()}, sum={output_parallel.sum().item()}")        # Mask the output embedding.        if self.tp_size > 1:            output_parallel.masked_fill_(input_mask.unsqueeze(-1), 0)        # Reduce across all the model parallel GPUs.        output = torch.ops.vllm.maybe_pad_and_reduce(output_parallel)        return outputclass PatchAscendParallelLMHead(AscendParallelLMHead):    """    Register ParallelLMHead as a custom op for Ascend."""    def __init__(        self,        num_embeddings: int,        embedding_dim: int,        bias: bool = False,        params_dtype: torch.dtype | None = None,        org_num_embeddings: int | None = None,        padding_size: int = DEFAULT_VOCAB_PADDING_SIZE,        quant_config: QuantizationConfig | None = None,        prefix: str = "",        uneven_split: bool = False,  # [h30014172] 新增    ):        PatchAscendVocabParallelEmbedding.__init__(        self,        num_embeddings,        embedding_dim,        params_dtype,        org_num_embeddings,        padding_size,        quant_config,        prefix,        uneven_split=uneven_split,  # [h30014172] 透传        )        self.quant_config = quant_config        if bias:            self.bias = Parameter(torch.empty(self.num_embeddings_per_partition, dtype=params_dtype))            set_weight_attrs(                self.bias,                {                    "output_dim": 0,                    "weight_loader": self.weight_loader,                },            )        else:            self.register_parameter("bias", None)class PatchAscendLogitsProcessor(AscendLogitsProcessor):    """    Register LogitsProcessor as a custom op for Ascend.    Added the feature of lmheadTP in pure dp scenario    """    def _get_logits(        self,        hidden_states: torch.Tensor,        lm_head: AscendParallelLMHead,        embedding_bias: torch.Tensor | None = None,    ) -> torch.Tensor | None:        if lmhead_tp_enable():            return self._get_logits_lmheadtp(hidden_states, lm_head, embedding_bias)        else:            return self._get_logits_normal(hidden_states, lm_head, embedding_bias)    def _get_logits_lmheadtp(        self,        hidden_states: torch.Tensor,        lm_head: AscendParallelLMHead,        embedding_bias: torch.Tensor | None,    ) -> torch.Tensor | None:        # Gather hidden states from all devices in tensor parallel group        gathered_hidden_states = get_lmhead_tp_group().all_gather(hidden_states, dim=0)        local_logits = lm_head.quant_method.apply(lm_head, gathered_hidden_states, bias=embedding_bias)        # Gather logits for tensor parallel        logits = get_lmhead_tp_group().all_to_all(local_logits)        # Remove paddings in vocab (if any)        if logits is not None:            logits = logits[..., : self.org_vocab_size]        return logits    def _get_logits_normal(        self,        hidden_states: torch.Tensor,        lm_head: AscendParallelLMHead,        embedding_bias: torch.Tensor | None,    ) -> torch.Tensor | None:        local_logits = lm_head.quant_method.apply(lm_head, hidden_states, bias=embedding_bias)        # Gather logits for tensor parallel        #errorlogger.debug(f"AscendLogitsProcessor--local_logits:{local_logits.shape} [h30014172]")        logits = self._gather_logits(local_logits)        # Remove paddings in vocab (if any)        if logits is not None:            logits = logits[..., : self.org_vocab_size]        return logits    # def _gather_logits(self, logits: torch.Tensor) -> torch.Tensor:    #     """gather/all-gather the logits tensor across model parallel group.            #     [h30014172] 非对称切分版本：padding 到相同大小后使用 all_gather    #     """    #     from vllm.distributed import get_tp_group, tensor_model_parallel_all_gather    #     tp_group = get_tp_group()    #     tp_rank = tp_group.rank_in_group    #     tp_size = tp_group.world_size    #     tp_asymmetric_shardings = [3, 3, 2]    #     local_size = logits.shape[-1]    #     base_size = local_size // tp_asymmetric_shardings[tp_rank]    #     max_size = max(tp_asymmetric_shardings[r] * base_size for r in range(tp_size))    #     if local_size < max_size:    #         logits = torch.nn.functional.pad(logits, (0, max_size - local_size))    #     gathered = tensor_model_parallel_all_gather(logits, dim=-1)    #     total_size = sum(tp_asymmetric_shardings[r] * base_size for r in range(tp_size))    #     gathered = gathered[..., :total_size]    #     return gathered